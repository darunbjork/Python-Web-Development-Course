# This line prompts the user to enter the first number and stores it as an integer in variable a
a = int(input("Enter the first number: "))

# This line prompts the user to enter the second number and stores it as an integer in variable b
b = int(input("Enter the second number: "))

# This line calculates the sum of a and b and stores the result in variable c
c = a + b

# This line prints a message "The sum is:" followed by the value stored in variable c
print("The sum is:", c)

